/** Shown in API response when company has no suggestions channel configured (Discord publish skipped). */
export const SUGGESTIONS_CHANNEL_NOT_CONFIGURED_WARNING =
  'Suggestions channel is not configured — the message will not be published to Discord.';
